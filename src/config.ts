
const config = {
  elevenLabsApiKey: "", // The user needs to provide their API key
  elevenLabsAgentId: "gTFIJ5j7KVQkDEZwGDps" // Default agent ID (user may need to replace with their own)
};

export default config;
